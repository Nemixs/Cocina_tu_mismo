<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mcl.cocina_tu_mismo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RefrigeradorActivity" />
        <activity android:name=".AlacenaActivity" />
        <activity
            android:name=".RecetasActivity"
            android:label="@string/title_activity_recetas"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ContactoActivity" />
        <activity android:name=".ExtrasActivity"></activity>
    </application>

</manifest>